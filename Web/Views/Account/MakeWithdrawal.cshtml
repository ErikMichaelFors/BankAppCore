@model Application.Models.AccountModels.WithdrawalViewModel

@{
    ViewData["Title"] = "MakeWithdrawal";
}

    <h1>Make Withdrawal</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Account" asp-action="MakeWithdrawal">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Account" class="control-label"></label>
                <input asp-for="Account" class="form-control" />
                <span asp-validation-for="Account" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Amount" class="control-label"></label>
                <input asp-for="Amount" class="form-control" />
                <span asp-validation-for="Amount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Withdrawal" class="btn btn-primary" />
            </div>
            @if (Model.StatusMessage != null)
            {
                <div class="form-group">
                    <strong class="text-danger">@Model.StatusMessage</strong>
                </div>
            }
        </form>
    </div>
</div>